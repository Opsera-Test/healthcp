# ════════════════════════════════════════════════════════════════════════════
# ArgoCD Application - health-11-dev
# RULE 59: Uses destination.name for spoke cluster reference
# ════════════════════════════════════════════════════════════════════════════
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: health-11-dev
  namespace: argocd
  labels:
    app: health-11
    environment: dev
    tenant: opsera
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: PLACEHOLDER_REPO_URL
    targetRevision: HEAD
    path: .opsera-health-11/k8s/overlays/dev
  destination:
    name: opsera-usw2-np
    namespace: opsera-health-11-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
