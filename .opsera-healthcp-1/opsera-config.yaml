# ════════════════════════════════════════════════════════════════════════════
# OPSERA CONFIG - Single Source of Truth (RULE 60)
# Generated by Code-to-Cloud Enterprise v0.903
# ════════════════════════════════════════════════════════════════════════════

version: "1.0"
generated: "2026-02-01"

# Application Configuration
app:
  name: healthcp-1
  type: frontend
  framework: vite-react
  port: 8080

# Tenant Configuration
tenant:
  name: opsera
  
# Cloud Configuration  
cloud:
  provider: aws
  region: us-west-2
  
# Cluster Configuration (RULE 70-72)
clusters:
  hub:
    name: argocd-usw2
    purpose: ArgoCD Control Plane
  spoke:
    name: opsera-usw2-np
    purpose: Workload Deployment
    
# ECR Configuration (RULE 101)
ecr:
  repository: opsera/healthcp-1
  
# DNS Configuration (RULE 64)
dns:
  domain: agent.opsera.dev
  pattern: "{tenant}-{app}-{env}.{domain}"

# Environments
environments:
  dev:
    namespace: opsera-healthcp-1-dev
    hostname: opsera-healthcp-1-dev.agent.opsera.dev
    replicas: 1
    strategy: rolling

# Deployment Settings
deployment:
  auto_promote: false  # RULE 65: Disabled until DEV verified
  health_check_path: /health
  
# Security
security:
  run_as_non_root: true
  read_only_root_filesystem: false
