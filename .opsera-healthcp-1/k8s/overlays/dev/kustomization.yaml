# ════════════════════════════════════════════════════════════════════════════
# KUSTOMIZATION - dev overlay
# Generated by Code-to-Cloud Enterprise v0.903
# ════════════════════════════════════════════════════════════════════════════

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: opsera-healthcp-1-dev

resources:
  - ../../base
  - namespace.yaml

commonLabels:
  environment: dev

# Image will be updated by CI/CD pipeline
images:
  - name: healthcp-1
    newName: PLACEHOLDER_ECR_URI
    newTag: latest

# Patch Ingress hostname for dev environment
patches:
  - target:
      kind: Ingress
      name: healthcp-1
    patch: |-
      - op: replace
        path: /spec/rules/0/host
        value: opsera-healthcp-1-dev.agent.opsera.dev
