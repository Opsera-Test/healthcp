# ════════════════════════════════════════════════════════════════════════════
# INGRESS - healthcp-1
# Generated by Code-to-Cloud Enterprise v0.903
# RULE 104: NO cert-manager or TLS section (HTTP-only routing)
# RULE 105: MUST have both ingressClassName AND annotation
# ════════════════════════════════════════════════════════════════════════════

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: healthcp-1
  labels:
    app: healthcp-1
    tenant: opsera
  annotations:
    # RULE 105: REQUIRED for backward compatibility
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "60"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "60"
    # NO cert-manager.io/* annotations (RULE 104)
spec:
  # RULE 105: REQUIRED for modern Kubernetes
  ingressClassName: nginx
  # NO spec.tls section (RULE 104)
  rules:
    - host: PLACEHOLDER_HOSTNAME
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: healthcp-1
                port:
                  number: 80
