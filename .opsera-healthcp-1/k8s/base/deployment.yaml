# ════════════════════════════════════════════════════════════════════════════
# DEPLOYMENT - healthcp-1
# Generated by Code-to-Cloud Enterprise v0.903
# RULE 80: imagePullPolicy: Always for Argo Rollouts compatibility
# RULE 81: imagePullSecrets REQUIRED for private ECR
# ════════════════════════════════════════════════════════════════════════════

apiVersion: apps/v1
kind: Deployment
metadata:
  name: healthcp-1
  labels:
    app: healthcp-1
    tenant: opsera
spec:
  replicas: 1
  selector:
    matchLabels:
      app: healthcp-1
  template:
    metadata:
      labels:
        app: healthcp-1
        tenant: opsera
    spec:
      # RULE 81: imagePullSecrets for private ECR
      imagePullSecrets:
        - name: ecr-credentials
      containers:
        - name: healthcp-1
          image: healthcp-1:latest
          # RULE 80: Always pull to ensure latest image
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              protocol: TCP
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 256Mi
          # Health checks (RULE 79: SPA health endpoint)
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          # Security context
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
