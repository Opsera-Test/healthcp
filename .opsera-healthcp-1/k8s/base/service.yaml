# ════════════════════════════════════════════════════════════════════════════
# SERVICE - healthcp-1
# Generated by Code-to-Cloud Enterprise v0.903
# RULE 23: ClusterIP for shared NGINX Ingress pattern
# ════════════════════════════════════════════════════════════════════════════

apiVersion: v1
kind: Service
metadata:
  name: healthcp-1
  labels:
    app: healthcp-1
    tenant: opsera
spec:
  # RULE 23: ClusterIP (NOT LoadBalancer) for shared ingress
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: healthcp-1
