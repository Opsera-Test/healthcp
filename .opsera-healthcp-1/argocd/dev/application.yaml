# ════════════════════════════════════════════════════════════════════════════
# ARGOCD APPLICATION - healthcp-1-dev
# Generated by Code-to-Cloud Enterprise v0.903
# RULE 59: Uses destination.name for hub-spoke architecture
# ════════════════════════════════════════════════════════════════════════════

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: healthcp-1-dev
  namespace: argocd
  labels:
    app: healthcp-1
    environment: dev
    tenant: opsera
    managed-by: opsera-c2c
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  source:
    # Will be updated by bootstrap workflow
    repoURL: https://github.com/opsera-agentic/healthcp
    targetRevision: HEAD
    path: .opsera-healthcp-1/k8s/overlays/dev

  destination:
    # RULE 59: CRITICAL - Use 'name' for spoke cluster, NOT 'server'
    # This ensures deployment goes to spoke cluster, not hub
    name: opsera-usw2-np
    namespace: opsera-healthcp-1-dev

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
