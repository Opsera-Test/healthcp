# ════════════════════════════════════════════════════════════════════════════
# ARGOCD APPLICATION - new-14-dev
# Generated by Opsera Code-to-Cloud Enterprise v0.914
# RULE 128a: ArgoCD application deployed via kubectl
# RULE 149: Uses destination.name for spoke cluster (NOT destination.server!)
# ════════════════════════════════════════════════════════════════════════════
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: new-14-dev
  namespace: argocd
  labels:
    app: new-14
    environment: dev
    tenant: opsera
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: PLACEHOLDER_REPO_URL
    targetRevision: HEAD
    path: .opsera-new-14/k8s/overlays/dev
  destination:
    # RULE 149: Use spoke cluster NAME, not https://kubernetes.default.svc
    name: opsera-usw2-np
    namespace: opsera-new-14-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
