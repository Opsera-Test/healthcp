# ════════════════════════════════════════════════════════════════════════════
# ARGOCD APPLICATION - DEV Environment
# Application: hcp-1 | Generated by Code-to-Cloud Enterprise v1.6
# RULE 59: Uses destination.name (NOT destination.server) for hub-spoke
# ════════════════════════════════════════════════════════════════════════════
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hcp-1-dev
  namespace: argocd
  labels:
    app: hcp-1
    environment: dev
    managed-by: opsera
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  
  source:
    # RULE 58: Resolved values (NOT placeholders)
    repoURL: https://github.com/opsera-agentic/healthcp.git
    targetRevision: main
    path: .opsera-hcp-1/k8s/overlays/dev
  
  destination:
    # TEMPORARY: Deploy to hub cluster until spoke cluster is registered
    # TODO: Register spoke cluster with ArgoCD and switch back to name: opsera-use1-np
    server: https://kubernetes.default.svc
    namespace: hcp-1-dev
  
  syncPolicy:
    # RULE 65: Auto-sync disabled until DEV verified working
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
