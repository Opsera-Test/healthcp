# ════════════════════════════════════════════════════════════════════════════
# KUBERNETES INGRESS - DEV Environment
# Application: hcp-1 | Generated by Code-to-Cloud Enterprise v1.6
# RULE 64: Domain is agent.opsera.dev (NOT agents.opsera.dev)
# ════════════════════════════════════════════════════════════════════════════
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hcp-1-frontend-ingress
  namespace: hcp-1-dev
  labels:
    app: hcp-1
    environment: dev
    managed-by: opsera
  annotations:
    kubernetes.io/ingress.class: nginx
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    # RULE 83: TLS certificate for this environment
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - hcp-1-dev.agent.opsera.dev
      secretName: hcp-1-dev-tls
  rules:
    - host: hcp-1-dev.agent.opsera.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: hcp-1-frontend
                port:
                  number: 80
