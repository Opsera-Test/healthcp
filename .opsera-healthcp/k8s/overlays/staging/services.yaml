# ════════════════════════════════════════════════════════════════════════════
# STAGING SERVICES - Active and Preview services for blue-green
# Generated by Opsera Code-to-Cloud Enterprise v1.4
# ════════════════════════════════════════════════════════════════════════════
apiVersion: v1
kind: Service
metadata:
  name: healthcp-active
  namespace: healthcp-staging
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: healthcp
---
apiVersion: v1
kind: Service
metadata:
  name: healthcp-preview
  namespace: healthcp-staging
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: healthcp
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: healthcp-active
  namespace: healthcp-staging
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
        - healthcp-staging.agents.opsera.dev
      secretName: healthcp-staging-tls
  rules:
    - host: healthcp-staging.agents.opsera.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: healthcp-active
                port:
                  number: 80
---
# Preview ingress for testing before promotion
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: healthcp-preview
  namespace: healthcp-staging
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
        - healthcp-staging-preview.agents.opsera.dev
      secretName: healthcp-staging-preview-tls
  rules:
    - host: healthcp-staging-preview.agents.opsera.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: healthcp-preview
                port:
                  number: 80
