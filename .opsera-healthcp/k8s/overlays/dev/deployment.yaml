apiVersion: apps/v1
kind: Deployment
metadata:
  name: healthcp
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: healthcp
          env:
            - name: ENVIRONMENT
              value: "dev"
