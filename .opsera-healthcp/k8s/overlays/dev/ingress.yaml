# ════════════════════════════════════════════════════════════════════════════
# KUBERNETES INGRESS - healthcp dev
# Generated by: Opsera Code-to-Cloud Enterprise v0.914
# Exposes the application via ExternalDNS
# ════════════════════════════════════════════════════════════════════════════
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: healthcp
  namespace: opsera-healthcp-dev
  labels:
    app: healthcp
    environment: dev
    tenant: opsera
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    external-dns.alpha.kubernetes.io/hostname: "opsera-healthcp-dev.agent.opsera.dev"
spec:
  ingressClassName: nginx
  rules:
    - host: opsera-healthcp-dev.agent.opsera.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: healthcp
                port:
                  number: 80
