# ════════════════════════════════════════════════════════════════════════════
# QA SERVICES - Canary and Stable services for traffic splitting
# Generated by Opsera Code-to-Cloud Enterprise v1.4
# ════════════════════════════════════════════════════════════════════════════
apiVersion: v1
kind: Service
metadata:
  name: healthcp-stable
  namespace: healthcp-qa
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: healthcp
---
apiVersion: v1
kind: Service
metadata:
  name: healthcp-canary
  namespace: healthcp-qa
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: healthcp
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: healthcp-stable
  namespace: healthcp-qa
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  tls:
    - hosts:
        - healthcp-qa.agent.opsera.dev
      secretName: healthcp-qa-tls
  rules:
    - host: healthcp-qa.agent.opsera.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: healthcp-stable
                port:
                  number: 80
