apiVersion: v1
kind: ServiceAccount
metadata:
  name: healthcp
  labels:
    app: healthcp
    tenant: opsera
    managed-by: opsera-c2c
# Option A: GitHub Secrets authentication (no IRSA annotation needed)
# AWS credentials passed via GitHub Secrets in workflows
