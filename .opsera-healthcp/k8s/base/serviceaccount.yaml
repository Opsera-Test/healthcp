apiVersion: v1
kind: ServiceAccount
metadata:
  name: healthcp
  # RULE 1: NO namespace field in base (kustomize overlay injects it)
  labels:
    app: healthcp
    tenant: opsera
    managed-by: opsera-c2c
