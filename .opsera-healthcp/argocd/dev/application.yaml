# ════════════════════════════════════════════════════════════════════════════
# ARGOCD APPLICATION - healthcp dev
# Generated by: Opsera Code-to-Cloud Enterprise v0.914
# RULE 149: Uses destination.name (spoke cluster) NOT destination.server
# ════════════════════════════════════════════════════════════════════════════
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: healthcp-dev
  namespace: argocd
  labels:
    app: healthcp
    environment: dev
    tenant: opsera
    app.kubernetes.io/managed-by: opsera
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/opsera-agentic/healthcp.git
    targetRevision: main
    path: .opsera-healthcp/k8s/overlays/dev
  # RULE 149: CRITICAL - Use destination.name for spoke cluster
  # NEVER use destination.server: https://kubernetes.default.svc (that's the hub!)
  destination:
    name: opsera-usw2-np
    namespace: opsera-healthcp-dev
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
