# ════════════════════════════════════════════════════════════════════════════
# ARGOCD APPLICATION - DEV Environment
# Hub-and-Spoke Model:
#   Hub (ArgoCD): argocd-use1 - where this Application CR lives
#   Spoke (Workload): opsera-use1-np - where pods are deployed
# Generated by Opsera Code-to-Cloud Enterprise v1.4
# ════════════════════════════════════════════════════════════════════════════
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: healthcp-dev
  namespace: argocd
  labels:
    app.kubernetes.io/name: healthcp
    environment: dev
    cluster: opsera-use1-np
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  
  source:
    repoURL: https://github.com/opsera-agentic/healthcp.git
    targetRevision: main
    path: .opsera-healthcp/k8s/overlays/dev
  
  destination:
    # Workload cluster registered in ArgoCD as 'opsera-use1-np'
    # This should match the cluster name registered via: argocd cluster add
    name: opsera-use1-np
    namespace: healthcp-dev
  
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
