apiVersion: v1
kind: Service
metadata:
  name: hcp-0305
  labels:
    app: hcp-0305
    tenant: opsera
# RULE 1: NO namespace field — overlays inject namespace
spec:
  selector:
    app: hcp-0305
  ports:
    - name: http
      port: 80
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
