apiVersion: v1
kind: ServiceAccount
metadata:
  name: hcp-0305
  labels:
    app: hcp-0305
    tenant: opsera
# RULE 1: NO namespace field — overlays inject namespace
# GitHub Secrets auth (default) — no IRSA annotation needed
