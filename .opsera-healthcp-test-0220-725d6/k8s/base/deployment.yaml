apiVersion: apps/v1
kind: Deployment
metadata:
  name: healthcp-test-0220-725d6
  namespace: opsera-healthcp-test-0220-725d6-uat
spec:
  replicas: 2
  selector:
    matchLabels:
      app: healthcp-test-0220-725d6
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: healthcp-test-0220-725d6
        version: "1.0"
    spec:
      serviceAccountName: healthcp-test-0220-725d6
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
      containers:
        - name: healthcp-test-0220-725d6
          image: PLACEHOLDER_ECR_URI
          ports:
            - containerPort: 8080
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
      imagePullSecrets:
        - name: ecr-secret
