# ════════════════════════════════════════════════════════════════════════════
# KUSTOMIZE BASE - new-16
# Generated by: Opsera Code-to-Cloud Enterprise v0.915
# RULE 106: Uses labels with includeSelectors: false to prevent selector mismatch
# ════════════════════════════════════════════════════════════════════════════
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - serviceaccount.yaml
  - deployment.yaml
  - service.yaml

# RULE 106: Use labels (NOT commonLabels) with includeSelectors: false
# This prevents kustomize from modifying selectors which causes 503 errors
labels:
  - pairs:
      app.kubernetes.io/name: new-16
      app.kubernetes.io/managed-by: opsera
    includeSelectors: false
