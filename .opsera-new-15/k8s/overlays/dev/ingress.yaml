# ════════════════════════════════════════════════════════════════════════════
# KUBERNETES INGRESS - new-15 dev
# Generated by: Opsera Code-to-Cloud Enterprise v0.914
# Exposes the application via ExternalDNS
# ════════════════════════════════════════════════════════════════════════════
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: new-15
  namespace: opsera-new-15-dev
  labels:
    app: new-15
    environment: dev
    tenant: opsera
  annotations:
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/proxy-body-size: "50m"
    external-dns.alpha.kubernetes.io/hostname: "opsera-new-15-dev.agent.opsera.dev"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - opsera-new-15-dev.agent.opsera.dev
      secretName: wildcard-agent-opsera-dev-tls
  rules:
    - host: opsera-new-15-dev.agent.opsera.dev
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: new-15
                port:
                  number: 80
